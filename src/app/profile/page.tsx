"use client";
import { ShieldCheck } from "lucide-react"; import ProtectedPage from "@/components/protected-page"; import { useAuth } from "@/components/providers"; import { StatusBadge } from "@/components/ui";
function Profile() { const { user } = useAuth(); return <><div><h1 className="font-display text-3xl font-extrabold text-navy">Profile</h1><p className="mt-2 text-sm text-muted">Your IJMS account and secure workspace information.</p></div><div className="mt-7 grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-xl border border-line bg-white p-6"><div className="flex items-center gap-4"><div className="grid h-14 w-14 place-items-center rounded-full bg-navy text-lg font-bold text-white">{user?.firstname?.[0]}{user?.lastname?.[0]}</div><div><h2 className="font-display text-xl font-bold text-navy">{user?.firstname} {user?.lastname}</h2><p className="text-sm text-muted">{user?.email}</p></div></div><dl className="mt-7 grid gap-5 border-t border-line pt-6 sm:grid-cols-2"><div><dt className="text-xs font-bold uppercase tracking-wider text-muted">Account role</dt><dd className="mt-1"><StatusBadge status={user?.role || "citizen"}/></dd></div><div><dt className="text-xs font-bold uppercase tracking-wider text-muted">Account status</dt><dd className="mt-1"><StatusBadge status={user?.is_active ? "active" : "inactive"}/></dd></div><div><dt className="text-xs font-bold uppercase tracking-wider text-muted">Email verification</dt><dd className="mt-1 text-sm font-medium text-ink">{user?.is_verified ? "Verified" : "Not verified"}</dd></div><div><dt className="text-xs font-bold uppercase tracking-wider text-muted">Account ID</dt><dd className="mt-1 text-sm font-medium text-ink">#{user?.id}</dd></div></dl></section><aside className="rounded-xl border border-line bg-white p-6"><ShieldCheck className="h-6 w-6 text-gold"/><h2 className="mt-4 font-display text-lg font-bold text-navy">Security</h2><p className="mt-2 text-sm leading-6 text-muted">Your session uses the active IJMS JWT authentication service. Password and email changes are not exposed by the current API.</p></aside></div></> }
export default function Page() { return <ProtectedPage><Profile/></ProtectedPage> }
