import { api } from "@/lib/api-client"; import type { Case, CaseInput } from "@/types";
export const casesService = { list: (token: string) => api<Case[]>("/api/v1/cases/", {}, token), get: (id: string, token: string) => api<Case>(`/api/v1/cases/${id}`, {}, token), create: (data: CaseInput, token: string) => api<Case>("/api/v1/cases/", { method: "POST", body: JSON.stringify(data) }, token), update: (id: number, data: Partial<CaseInput & Pick<Case, "status">>, token: string) => api<Case>(`/api/v1/cases/${id}`, { method: "PATCH", body: JSON.stringify(data) }, token), remove: (id: number, token: string) => api<void>(`/api/v1/cases/${id}`, { method: "DELETE" }, token) };
