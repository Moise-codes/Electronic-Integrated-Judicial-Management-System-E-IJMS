import { ServerOff } from "lucide-react"; import ProtectedPage from "@/components/protected-page"; import { EmptyState } from "@/components/ui";
export default function UnavailablePage({ title, description }: { title: string; description: string }) { return <ProtectedPage><div><h1 className="font-display text-3xl font-extrabold text-navy">{title}</h1><p className="mt-2 text-sm text-muted">{description}</p></div><div className="mt-7"><EmptyState title={`${title} are not connected yet`} description="The current IJMS API does not publish endpoints for this resource. This interface will become available when the corresponding backend routes are enabled." action={<ServerOff className="mx-auto h-5 w-5 text-muted"/>}/></div></ProtectedPage> }
